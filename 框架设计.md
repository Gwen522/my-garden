# 个人网站 · 框架设计

> 版本：v0.2（2026-09-20）
> 定位：一座自我空间 / 数字居所 —— 记录、回顾、探索自己，随生活生长。

## 1. 定位与愿景

- 它首先是一片给自己养的自留地，其次才是公开的展示页。
- 居所隐喻：网站 = 一座居所，每个"房间" = 一个可插拔模块。
- 三个关键词：
  - **较大的框架**：稳定壳 + 可插拔模块，不做一个页面，而是一个能长期生长的体系。
  - **动态、完整**：静态托管到 GitHub Pages，但前端动态渲染 + 时间引擎，每时每刻都是新的。
  - **方便插拔**：内容与代码分离；房间、主题、对象随时增删，核心不动。

## 2. 三条支柱

| 支柱 | 含义 |
| --- | --- |
| 内容契约 | 所有内容 = 带统一字段的对象；管理面板、通用算法、展示端共用同一套定义 |
| 时间引擎 | 内容带时间戳，展示是时间的函数：过去 / 现在 / 将来 = 同一时间轴上的时间窗 |
| 可插拔模块 | 核心稳定，房间可增删；新部分 = 新对象 + 新房间 + 新组件，注册进去即可 |

## 3. 架构总览（单仓库 · 两个面）

```
同一个 GitHub 仓库 = 唯一事实源（内容 Markdown + 代码 Vue + 管理面板）
├─ 公开面：GitHub Pages 静态站点 —— 只读展示，访客只能看到渲染结果
└─ 私密面：本地面板（形态 A）—— 本地运行的网页，写内容 + 保存并发布
```

- 发布管道：面板保存 → 以你的 GitHub 身份提交推送 → Actions 自动构建部署 → 网页更新。
- 权限模型：能改 = 你的 GitHub 身份；访客永远只碰到展示面。
- 面板形态 A（本地面板，已定）：只在你电脑上跑（127.0.0.1），不部署上网，别人碰不到，零登录配置。
- 未来升级路径：若内容需要文件级私密 → 双仓库（私密内容仓库 + 公开展示仓库），面板提交目标改为私密仓库并同步公开部分，架构不推翻。

## 4. 主题系统

- 主题 = 一组设计令牌（背景、文字、强调色、字体、氛围），内容与风格完全分离。
- 第一版两套：**纸墨**（宣纸底 · 墨字 · 衬线标题 · 黛蓝点缀，典雅文人向）、**霓虹**（深空底 · 青紫霓虹 · 等宽数字，赛博朋克向）。
- 两个维度：**风格**（纸墨 / 霓虹，管理端可切，按心情）＋ **明暗**（浅色 / 深色，访客侧"昼夜"按钮，偏好记住）。
- 加新风格 = 加一组令牌，不碰内容与逻辑。

## 5. 对象模型（第一版）

| 对象 | 字段 | 住的房间 |
| --- | --- | --- |
| 设置 Settings | 站名、关于我、生日、建站日、时间窗长度（默认 3 个月）、默认主题 | 全站 |
| 内容 Content | 日期、类型（diary/essay/game/anime/travel/poetry…）、标题、正文、可见性、标签 | 心斋 / 闲斋 / 足迹 / 书斋 |
| 节点 Node | 日期、标题、描述、地点（可选）、图片（可选） | 正厅·回廊 / 足迹 |
| 记录 Record | 日期、内容 | 暂缓（热力图数据源） |
| 目标 Goal | 标题、目标日期、状态 | 暂缓（愿望墙） |
| 胶囊 Capsule | 写于、开封日期、正文、可见性 | 暂缓 |

原则：**对象只存事实，通用方法做计算，展示只读结果。**

## 6. 通用方法（时间计算）

- 年龄（生日）· 距今 / 倒计时（日期）· 至今展开（时期）· 时间窗归类 · 开封判断 · 连续统计 · 期间时长

## 7. 房间地图 v2

| 房间 | 内容 | 状态 |
| --- | --- | --- |
| 正厅（首页） | 关于我 · 动态数字（年龄 / 园龄）· 此刻 · 回廊（人生历程时间线）· 最近日记 | 已规划 |
| 心斋 | 日记 · 随笔 · 诗词摘抄（可选） | 已规划 |
| 闲斋 | 游戏成就 · 看过的动漫 · 玩过的东西 | 已规划 |
| 足迹 | 旅游 · 去过的地方（带地点与照片） | 已规划 |
| 书斋 | 读书笔记 | 暂缓，等想好放什么 |
| 藏阁、祈愿亭 | — | 暂缓不做 |

回廊并入正厅，作为人生历程的时间线段落；节点对象天然支持足迹（有地点的节点 = 足迹记录）。

## 8. 动态机制

- 「现在」= 最近 N 个月的时间窗（默认 3，管理端可配置），内容按时间戳自动归类，过期自动滑入过去。
- 时期结束日期留空 = "至今"，展示时实时计算。
- 动态数字（年龄、园龄、倒计时）只写一次，每次打开页面自动重算。
- 站名 = 设置项，管理端随时可改（不阻塞：现用占位名，园名方向偏好宇宙感，备选：一苇 / 白露 / 一粟 / 清风明月）。
- 所有计算发生在打开页面的那一刻，不需要服务器。

## 9. 增量实现与验收（开发节奏）

- 原则：一小块一小块做，每块做完验收；验收通过再进下一块。
- M0 地基 ✅：仓库 + Vue 壳 + 内容契约（设置 / 内容）+ 部署流水线，已上线。
- M0 收尾（进行中）：本地面板 —— 写内容、改设置、保存并发布，封装 git 提交。
- M1：心斋 + 主题系统（纸墨 / 霓虹 / 昼夜）。
- M2：正厅回廊（时期 + 节点时间线）+ 足迹。
- 随缘：书斋、愿望墙、热力图（等自然数据源）。

## 10. 暂缓与待定（Open Questions）

- 园名：未定（管理端可随时改）。
- 热力图：暂缓，等自然数据源（本站 git 提交记录可自动生成；外部数据不承诺）。
- 站内 /admin（形态 B）：将来想用手机写日记时再做。
- 书斋内容、更多房间与字段：随写随加。
